@model IEnumerable<TrainMVC.ViewModel.GetTrainIFOViewModel>

@{
    ViewBag.Title = "GetTrainIFO";
}

    <h2>車次</h2>@ViewBag.TrainID

<p>
    
</p>
<table class="table">
    <tr>
        <th>
            站名
        </th>
        <th>
            到達時間
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StationCTName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ARRTime)
        </td>

    </tr>
}

</table>
